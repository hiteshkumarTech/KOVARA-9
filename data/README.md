# Data policy

Phase 0 has no external dataset. Procedural episodes are generated from explicit configurations
and seeds. Generated trajectories, evaluation runs, and future model artifacts belong under the
ignored `runs/` directory or an explicitly configured external artifact root.

Never commit private data, credentials, unreviewed binary artifacts, or fabricated benchmark
results here.
